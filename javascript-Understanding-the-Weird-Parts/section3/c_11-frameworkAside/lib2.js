/**
 * if you see this in a library or frame work, this is checking to make sure we aren't colliding 
 * or over ride it with an existing global variable/namespace
 */
window.libraryName = window.libraryName || "Lib 2";