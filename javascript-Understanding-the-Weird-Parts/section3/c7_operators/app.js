// this plus sign is an operator and is actually a function
// the js engine provides the ability to write whats is called infix notation, means 
// the operator sits between two parameters ex: var a below
var a = 4 + 3;
/*
 * whats happening behind the scenes
 * function +(a, b) {
 *  return // add the two #'s
 * }
 */
console.log(a);