// write a loop that makes seven calls to to console.log(); to output triangle
var hashSymbol = '#';

for (hashSymbol; hashSymbol.length <= 7; hashSymbol += '#') {
  console.log(hashSymbol);
}

/*

// example using while and for
// using while loop to output each result plus 2
count = 0;
while (count <= 12) {
  console.log(count);
  count = count + 2;
}
console.log('-------------');

// using for loop to output each result plus 2
for (var count = 0; count <= 12; count = count + 2) {
  console.log(count);
}

// using for loop to output each result times 2
var result = 1 ;
for ( var counter = 0 ; counter < 10 ; counter = counter + 1 ) {
  result = result * 2;
  console.log( result );
}
*/
